[project]
name = "clichat"
version = "0.1.2"
description = "CLI chat tool for local (Ollama, OMLX) and cloud (Gemini, OpenRouter, NVIDIA) models"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=2.23.0",
    "openai>=3.14.1",
    "prompt-toolkit>=3.0.53",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pyyaml>=6.0.3",
    "rich>=15.0.0",
]

[project.scripts]
clichat = "clichat.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
